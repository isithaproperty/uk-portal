import {env} from "cloudflare:workers";
export async function setupContractors(){await env.DB.batch([env.DB.prepare("CREATE TABLE IF NOT EXISTS contractors (id TEXT PRIMARY KEY, name TEXT NOT NULL, trade TEXT NOT NULL, email TEXT NOT NULL, phone TEXT, coverage TEXT, status TEXT NOT NULL DEFAULT 'Approved', notes TEXT, created_at INTEGER NOT NULL)"),env.DB.prepare("CREATE TABLE IF NOT EXISTS contractor_documents (id TEXT PRIMARY KEY, contractor_id TEXT NOT NULL, document_type TEXT NOT NULL, file_name TEXT NOT NULL, storage_key TEXT NOT NULL, expiry_date TEXT, uploaded_at INTEGER NOT NULL)"),env.DB.prepare("CREATE INDEX IF NOT EXISTS contractors_trade_idx ON contractors(trade, name)"),env.DB.prepare("CREATE INDEX IF NOT EXISTS contractor_documents_contractor_idx ON contractor_documents(contractor_id, uploaded_at DESC)")])}
