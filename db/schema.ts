import {integer,sqliteTable,text} from "drizzle-orm/sqlite-core";
export const workOrders=sqliteTable("work_orders",{id:text("id").primaryKey(),reference:text("reference").notNull(),title:text("title").notNull(),building:text("building").notNull(),contractor:text("contractor").notNull(),contractorEmail:text("contractor_email").notNull(),priority:text("priority").notNull(),status:text("status").notNull(),description:text("description"),dueDate:text("due_date"),invoiceName:text("invoice_name"),invoiceKey:text("invoice_key"),invoiceStatus:text("invoice_status").notNull().default("Awaiting invoice"),createdAt:integer("created_at").notNull()});
